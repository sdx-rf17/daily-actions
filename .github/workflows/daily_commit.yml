name: Super Green Daily Commits 🌿

on:
  schedule:
    - cron: '0 0 * * *'     # 00:00 UTC
    - cron: '4 4 * * *'     # 04:04 UTC
    - cron: '8 8 * * *'     # 08:08 UTC
    - cron: '12 12 * * *'   # 12:12 UTC
    - cron: '16 16 * * *'   # 16:16 UTC
  workflow_dispatch:        # Allows manual triggering

jobs:
  commit_spammer:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v3

    - name: Install dependencies (jq for JSON parsing)
      run: sudo apt-get install -y jq

    - name: Set Git Identity (REQUIRED)
      run: |
        git config user.name "sdx-rf17"
        git config user.email "sadex7rafaa@gmail.com"

    - name: Make 3 commits
      run: |
        for i in {1..3}; do
          DATE=$(date -u +"%Y-%m-%d %H:%M:%S UTC")
          QUOTE=$(curl -s https://api.quotable.io/random | jq -r '.content + " — " + .author')

          echo "### $DATE" >> commit_log.md
          echo "$QUOTE" >> commit_log.md
          echo "" >> commit_log.md

          git add commit_log.md
          git commit -m "🤖 Commit #$i on $DATE: $QUOTE"
          sleep 5
        done

    - name: Push commits to GitHub
      run: git push
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
